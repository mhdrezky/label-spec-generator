# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Extraction results (includes per-run llm/ stage cache)
results/

# Benchmark predictions + per-image llm cache (regenerated by benchmark/run_benchmark.py)
# Also covers ad-hoc comparison snapshots (predictions_baseline/, predictions_v2/, etc.)
benchmark/predictions/
benchmark/predictions_*/

# Editor preview copy (regenerated by main.py)
editor/latest-specs.json
